# 关于「不公开启动校验处理方式」的说明

本仓库只包含 QQNT 插件宿主的框架代码（加载插件、渲染进程注入、设置界面集成），
**不包含**任何用于绕过 QQ 启动期文件校验的二进制、生成器、安装脚本或目标 DLL 名称。

具体地，以下内容一律不入库（部分已由 `.gitignore` 兜底）：

- `bin/`（补丁脚本、代理 DLL、helper DLL、内置解压器、启动器脚本等）
- 一切安装 / 卸载脚本（如 `install.ps1`、`*.cmd`、`*.vbs`）
- 官方二进制的副本、地址偏移、特征码、调用序列
- 本机运行产物：`logs/`、`data/`、`backup/`、`out/`

公开版本的 `src/main.js` 中，启动自检已裁剪为只报告宿主自身状态，
不再包含任何与校验处理方式有关的路径或名称。

原因：绕过实现一旦公开，官方可以按其细节做定向封堵；把它留在仓库之外，
既能让框架部分持续维护，也不会让整个项目因为一个文件名、一个偏移或一个调用序列而失效。

贡献与使用约定：

1. 提交 PR 时不要附带任何绕过实现、QQ 官方二进制或其副本；
2. 不要在 issue 里贴出注入/补丁细节（可私信维护者）；
3. 插件作者只需依赖 `src/` 暴露的 API，不需要知道绕过细节；
4. 本仓库为「部分公开，仅作技术学习」，请勿用于商业分发。

> 上述裁剪只影响仓库内容，不代表功能清单：宿主的插件加载、面板、多账号、日志等能力均在 `src/` 内完整可见。
